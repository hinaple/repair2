{
    "compilerOptions": {
        "ignoreDeprecations": "6.0",
        "baseUrl": "../",
        "paths": {
            "@classes/*": ["../classes/*"]
        }
    },
    "exclude": ["node_modules", "dist"]
}
